# Bring-up status

This is a clean-room downstream BSP, not a repackaged vendor image.

Target: GA36-MB V1.2 (R36S), Allwinner A33. The recovered fex
(`output/sys_config.fex`) is the frozen hardware spec.

Strategy: **keep the stock Allwinner bootloader** (BROM → boot0/boot1 → stock
U-Boot → Android boot image) and replace only the kernel. A full mainline
SPL+U-Boot port was attempted and abandoned (see `docs/spl-vs-boot0-audit.md`
for why). The build produces exactly **one** image,
`output/firmware/ga36-stockboot.img`.

| Area | State | Evidence / next gate |
|---|---|---|
| Original-media partition map | discovered | `docs/analysis.md` generated by `tools/forensics/analyze.sh` |
| Automated extraction | implemented | `tools/forensics/extract.sh`, read-only input |
| Hardware spec | frozen | `output/sys_config.fex`, `output/hardware-report.md` |
| Stock boot chain preserved | committed | `bootloader/ga36-stock-bootchain-128m.bin.gz` (first 128 MiB of the factory image, verbatim) |
| Linux 6.12.41 | built | `scripts/fw/build-linux.sh`, `dts/sun8i-a33-ga36-mb-v1.2.dts`, saved `board/ga36-mb-v1.2/linux-ga36.config` |
| Rootfs (static BusyBox) | built | `scripts/fw/build-initramfs.sh`, ext4 rootfs, getty on ttyS2 + tty1 |
| Stock-bootloader SD image | built, **self-verifying** | `scripts/fw/package-stock.sh`, `output/firmware/ga36-stockboot.img` (boot0@16 / boot1@38192 / env@139264 preserved, Android boot img @**172032**, rootfs @262144) |
| ~~Android boot img offset bug~~ | **found & fixed** | commit `7ead1c8` had regressed `BOOT_IMG_LBA` to `172031` (off-by-one). The stock bootloader reads the kernel from LBA **172032** (see `docs/GA36-MB-Linux`, `dd … seek=172032`). Fixed back to `172032` in `scripts/fw/package-stock.sh`. Before this fix the kernel was never loaded → stuck on the splash logo. |
| Display (JD9366 DSI) | Route A kernel ready — test on silicon | exact vendor DCS init extracted from the stock `lcd.ko` (panel driver `jd9366_8inch` = patched `lp079x01.c`), committed to `board/ga36-mb-v1.2/jd9366_init.h`; regenerable via `scripts/fw/recover-lcd-dcs.sh` (hash-pinned); DRM panel driver `boe,jd9366` wired into the DSI/TCON. fex says DSI video mode, 2 lanes, RGB888, dclk 30 MHz, ht 1040 / vt 518; clock model = mainline (360 MHz/lane, PLL3 360 MHz). Flash `ga36-stockboot.img`, expect backlight + fbcon on LCD |
| OTG / SD2 / FN bindings | intentionally unassigned | must never be guessed from board family similarity |
| Buildroot rootfs | dropped | the build only needs the static-BusyBox rootfs; the old buildroot external tree was removed |

Config base: `sunxi_defconfig` (`board/ga36-mb-v1.2/linux-ga36.config`) with
fex rail values (DCDC1/2/3/5 = 3.3/1.1/1.26/1.35 V). The kernel relies on the
stock boot0 having initialised DRAM; the DTS has no `/memory` node (the size
is fixed up by the bootloader via ATAGs, `CONFIG_ARM_ATAG_DTB_COMPAT`).

Kernel cmdline is forced (`CONFIG_CMDLINE_FORCE`):
`root=/dev/mmcblk0p1 rootfstype=ext4 rootwait rw init=/sbin/init
earlycon=uart,mmio32,0x01c28800 loglevel=8 panic=10` — the stock bootloader's
own bootargs are ignored.

## Build

**Single entry point** `./build.sh` (bootstrap once with `./bootstrap.sh`).
Pipeline: `build-linux.sh` → `build-initramfs.sh` → `package-stock.sh`.
Artifacts land in `output/firmware/`.

## Verification rule (this session onward)

Static analysis (disassembly, file reads, offset math) proves the **file**
is correct — never that the **hardware** ran it. A fix is a *hypothesis*
until a hardware test changes the observable behaviour. `package-stock.sh`
verifies at build time: boot0 eGON checksum, `ANDROID!` magic @172032, MBR
signature, partition start.
