# Contributing

Keep board-specific work under `board/ga36-mb-v1.2`, `dts/`, and `uboot/`. Do not change an upstream board as a shortcut. Every hardware claim needs a reproducible evidence reference in `docs/hardware-notes.md`.
